{
  "name": "firebase-chat-app",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:client": "npm --prefix packages/client run build",
    "build:server": "npm --prefix packages/server run build",
    "clean": "npm-run-all clean:*",
    "clean:client": "npm --prefix packages/client run clean",
    "clean:server": "npm --prefix packages/server run clean",
    "deploy": "npm-run-all clean:* && npm-run-all build:* && firebase deploy",
    "deploy:client": "yarn clean:client && yarn build:client && firebase deploy --only hosting",
    "deploy:server": "yarn clean:server && yarn build:server && firebase deploy --only functions"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  }
}
